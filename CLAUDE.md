# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`boukgg/bouk100`) is currently a fresh, empty project. As of this writing it contains only:

- `README.md` — a single-line title (`# Bouk100`)
- `CLAUDE.md` — this file

There is **no source code, build system, test suite, dependency manifest, or CI configuration yet**. Consequently there are no build/lint/test commands to document, and no architecture to describe.

## When code is added

The project scaffold has not been established, so the language, framework, and tooling are still undecided. Once the first real code lands, **update this file** to capture the details that are not obvious from a quick look at the tree:

- **Common commands** — how to install dependencies, build, run the app, lint, and run the test suite (including how to run a *single* test).
- **Architecture** — the big-picture structure and how the major pieces fit together, i.e. the things that require reading several files to understand.
- **Conventions** — any non-standard patterns, project-specific idioms, or workflow requirements a new contributor would otherwise have to discover the hard way.

Do not add these sections speculatively; add them only when they reflect code that actually exists.

## Git workflow

- Feature development for this task happens on the branch `claude/claude-md-documentation-anlc9m`.
- The default branch is `main`.
- Do not open a pull request unless explicitly asked.
